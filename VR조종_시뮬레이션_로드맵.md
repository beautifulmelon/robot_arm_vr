# VR 조종 시뮬레이션 로드맵

> 작성 2026-08-08 · **갱신 2026-08-08 (실제 구조 반영)**
> 초판은 CloudXR 을 전제로 썼는데, 실제로는 **더 나은 세 번째 길**로 이미 구현·검증됐다.
> 그 결과 초판의 "관문"이던 Phase 2 가 통째로 사라졌다. 이 문서는 실제에 맞춰 다시 썼다.

---

## 0. 가진 기기 & 네트워크

| 장소 | 기기 |
|---|---|
| **작업장** | 맥북, Quest 2, 실물 로봇 + Amazing Hand, 젯슨 오린 나노, 카메라 2개 |
| **집(원격)** | 데스크탑 RTX 5070 12GB, Ubuntu 22.04 — Tailscale `100.93.186.122` |

**Tailscale 실측 (2026-08-07, direct 연결)**

```
왕복  최소 26.3 / 평균 41.3 / 최대 120.0 ms,  지터 21.6 ms,  손실 0%
```

> ⚠️ 초판에 적힌 "~20ms" 는 낙관적이었다. **실측은 2배**다.
> 이 숫자가 CloudXR 을 안 쓰기로 한 근거의 하나다 (§2).

---

## 1. 실제 구조 — 영상이 아니라 숫자가 인터넷을 건넌다

```
[작업장]                                        [집]
 Quest 2 ──WebXR 90Hz(같은 Wi-Fi)──▶ 맥북        데스크탑 RTX 5070
                                      │           · Isaac Sim 5.1.0 (시뮬 로봇)
                     좌표변환·클러치·IK│           · vr_bridge (UDP 수신 → 관절 구동)
                                      │           · lerobot (수집·학습)
                        UDP 5015 지령 ├──────────▶ 관절각 5개 + 서보각 8개
                        UDP 5016 상태 ◀──────────┤ 실제 관절각 17개
                                      │
                       VR HUD 로컬 렌더┘  (지연 0 — 인터넷을 안 건넘)

 화면 확인: 맥북에서 RustDesk 로 Isaac 창을 띄우고,
            헤드셋을 살짝 걸쳐 아래로 화면을 보며 조종한다.
```

**초판(CloudXR)과의 차이 — 여기가 핵심이다**

| | 초판 (CloudXR) | 실제 |
|---|---|---|
| 인터넷을 건너는 것 | **영상** (motion-to-photon) | **숫자** (관절각 13개, 패킷 ~200B) |
| IK 를 푸는 곳 | 집 데스크탑 (Isaac Teleop) | **맥북** — 실물과 같은 코드 |
| 지연 민감도 | 매우 높음 (멀미) | 낮음 — 지령만 늦고 화면은 로컬 |
| 필요한 것 | CloudXR, Quest 를 Tailscale 에, Isaac Teleop, LeIsaac | **없음** |

> ★ **sim-to-real 관점에서 이게 더 중요하다.**
> 초판 방식은 데스크탑의 Isaac Teleop 이 IK 를 푼다. 그러면 실물이 쓰는 IK 와
> **다른 해**가 나온다. 5축 팔은 6자유도 목표에 대한 해가 하나가 아니라 반드시
> 갈라진다. 그러면 "시뮬은 되는데 실물은 안 된다"의 원인을 못 가린다.
> 실제 구조는 **같은 맥북 코드·같은 IK·같은 프로토콜**이 시뮬과 실물을 몬다.

---

## 2. 진행 현황

| Phase | 내용 | 상태 |
|---|---|---|
| **1** | 스택 설치 | ✅ Isaac Sim 5.1.0 · lerobot · PyTorch 2.7+cu128(sm_120) |
| **2** | ~~CloudXR 연결·지연 실측~~ | **불필요해짐** — §1 구조로 대체 |
| **3** | VR 로 시뮬 로봇 조종 | ✅ **팔·손 모두 검증 완료** |
| **4** | 데이터 수집 | ⏳ 다음 차례 |
| **5** | 학습 | ⏳ (환경은 준비됨) |
| **6** | 시뮬 평가 | ⏳ |
| **7** | Sim-to-Real | ⏳ 실물은 CAN 어댑터 대기 · 절차는 §4 |

**Phase 1 — 설치된 것 / 안 쓴 것**

```
설치됨   Isaac Sim 5.1.0  (~/projects/isaac-sim/.venv, Python 3.11)
         lerobot          (~/projects/lerobot/.venv, torch 2.11+cu128)
안 씀    Isaac Lab · LeIsaac · Isaac Teleop · CloudXR
         → §1 구조에서는 전부 불필요. Phase 4 에서 lerobot 형식만 맞추면 된다.
```

**Phase 3 — 검증 실측**

```
팔     지령 813개 / 30Hz, link ok 0~34ms, trip 0, 추종오차 ~1°
       실제 Quest 조종: 지령 9,620개 / 상태 5,629개 / link ok 7.3ms
관절순서 one-by-one 스윕에서 joint1~5 가 순서대로만 움직임 (수치 확인)
손     grasp 0.04 → 검지 굴곡 -26.5° / 0.98 → +44.2°  (문서값과 일치)
손목롤 축을 URDF 에서 실측 → 양쪽 팔 모두 자세오차 0.00°
       범위: 5축 -189.6°~+166.4° / 임시팔 -175.6°~+180.4°
```

**Phase 3 에서 찾아 고친 것**

- **HOLD 가 홀드를 안 함** — 매 프레임 목표를 현재 위치로 덮어 복원 토크가 사라졌다.
  72초에 걸쳐 134.2° 붕괴 → 수정 후 90초에 0.36°.
- **손목 롤이 엉뚱한 축** — EE 로컬 X 를 돌리라고 요청했는데 실제 축은 Y.
  음의 방향이 죽어 "한쪽으로만 돌던" 증상. 축을 URDF 에서 뽑아 해결.

---

## 3. 남은 단계

### Phase 4 — 데이터 수집 (다음 차례)
집 데스크탑에서 Isaac 이 카메라를 렌더해 **그 자리에서 디스크에 저장**한다.
관찰(이미지+관절각)과 행동(지령)을 lerobot 형식으로 기록.

- ★ **데이터가 인터넷을 한 번도 안 건넌다.** 데이터도 학습도 같은 기계다.
- 조종자 시야는 RustDesk 로 충분하므로 영상 스트리밍 설계가 필요 없다.
- 필요한 것: 씬에 물체(큐브 60~80mm) · 카메라 2대(본체 후상방 + 손목) · 기록 계층

**✅ 검증**: 에피소드가 lerobot 데이터셋으로 저장 / 위치를 바꿔가며 대량 수집

### Phase 5 — 학습 (같은 데스크탑)
```bash
lerobot-train --dataset.repo_id=<계정>/<데이터셋> --policy.type=act \
  --output_dir=outputs/train/vr_sim --policy.device=cuda
```
**✅ 검증**: loss 감소 + 체크포인트. VRAM 12GB 라 headless·작은 배치로 시작.

### Phase 6 — 시뮬 평가 (헤드리스)
```bash
lerobot-eval --policy.path=outputs/train/vr_sim/checkpoints/last --eval.n_episodes=...
```
**✅ 검증**: 자동 rollout 성공률.

### Phase 7 — Sim-to-Real
절차는 §4 에 따로 정리했다.

---

## 4. Sim-to-Real — 시뮬과 실물이 다른 것을 어떻게 메우나

> **"각도가 약간 다른 것"은 사실 가장 작은 문제다.** 순서가 중요하다.
> 아래는 큰 것부터다.

### 4-1. 영점·부호 — 제일 먼저. 이게 안 맞으면 나머지가 전부 무의미하다

URDF 의 `joint1 = 0` 과 실물 모터의 `0` 이 **같은 자세가 아니다.** 조립 공차,
엔코더 원점, 기어 조립 위상 때문이다. 부호도 반대일 수 있다.

- 5° 어긋나면 그 아래의 어떤 보정도 의미가 없다. 정책은 "이 화면에서 이 각도"를
  배웠는데 같은 각도가 실물에서 다른 자세이기 때문이다.
- **방법**: 실물 팔을 기준 자세에 놓고 엔코더를 읽어 관절별 오프셋·부호 표를 만든다.
  그 표를 젯슨 쪽 변환에 넣어, **UDP 로 오가는 관절각이 항상 URDF 좌표**가 되게 한다.
- 젯슨 쪽 대기 목록에 이미 "joint3 고정 각도 / 부호·영점 표"가 있다. 그게 이것이다.

### 4-2. 액션 공간 — 이미 같다 (이 구조가 공짜로 주는 것)

시뮬과 실물이 **같은 맥북 코드에서 나온 같은 관절각**을 받는다. 프로토콜도 같다.
손도 서보각 8개로 같다 (관절각으로 보내면 실물에 없는 자세가 나온다 — 리밋
사각형의 55%가 도달 불가).

대부분의 sim-to-real 프로젝트가 여기서 고생하는데, 우리는 구조적으로 없다.

### 4-3. 동역학 갭 — 위치 지령이라 상당 부분 흡수된다

정책이 내는 것은 **토크가 아니라 목표 관절각**이다. 그 아래의 PD 드라이브(시뮬) /
서보 컨트롤러(실물)가 차이를 흡수한다. 그래서 위치제어 모방학습이 토크제어보다
훨씬 잘 넘어간다.

남는 것은 **응답 속도·지연·마찰**이다. 두 가지로 대응한다.

1. **게인 정합** — 실물 팔의 스텝 응답을 재서 Isaac 의 stiffness/damping 을 맞춘다.
   지금 값(팔 1000/50, 손 1000/20)은 시뮬에서 안 떨리게 고른 값이지 실물 정합값이 아니다.
2. **도메인 랜덤화** — 에피소드마다 stiffness·damping·마찰·질량을 흔든다.
   그러면 정책이 하나의 로봇이 아니라 **범위**에서 도는 법을 배우고, 실물이 그
   범위 안에 들어온다. 정합보다 이쪽이 실무적으로 강하다.

> ★ 젯슨 팀이 실기에서 찾은 것들이 여기 직결된다.
> `velocity_scale` 이 속도가 아니라 사실상 **토크 상한**으로 작동한다는 것,
> 마찰이 큰 팔은 scale 0.1 에서 아예 안 움직인다는 것.
> **시뮬에 마찰을 안 넣으면 이 구간이 통째로 안 보인다.**

### 4-4. 인식 갭 — 진짜 난관은 여기다

카메라 이미지가 시뮬과 실물에서 제일 많이 다르다 (조명, 질감, 노이즈, 렌즈 왜곡).
정책의 판단 근거가 영상이므로 여기가 어긋나면 관절각이 아무리 맞아도 실패한다.

- **시각 도메인 랜덤화** — 조명 방향·세기, 물체 텍스처·색, 카메라 위치를 조금씩 흔든다.
- 손목 카메라는 배경이 덜 변해 상대적으로 유리하다.

### 4-5. 실물 소량 파인튜닝 — 실무적으로 가장 확실한 마무리

시뮬 수백 에피소드로 학습 → **실물 10~30 에피소드로 파인튜닝.**
4-1~4-4 로 좁혀놓은 뒤 이걸 하면 적은 실물 데이터로 붙는다.

### 4-6. ★ 우리만의 무기 — 갭을 추측하지 않고 **잰다** (디지털 트윈)

프로토콜이 같으므로 **같은 지령을 실물과 시뮬에 동시에 보낼 수 있다.**
실제 관절각을 나란히 받아 비교하면 **갭이 숫자로 나온다.**

```
맥북 ─┬─ UDP 5005 ─▶ 젯슨 ─▶ 실물 팔 ──▶ 실제 관절각
      └─ UDP 5015 ─▶ 브릿지 ─▶ 시뮬 팔 ─▶ 실제 관절각
                                          └▶ 차이 = 갭
```

이걸로 얻는 것:
- **영점 오차** (4-1) 가 상수 오프셋으로 바로 드러난다
- **동역학 갭** (4-3) 이 지연·오버슈트 형태로 드러난다 → 게인 정합의 목표값
- **도메인 랜덤화 범위** (4-3) 를 추측이 아니라 실측으로 정한다

★ CAN 어댑터가 꽂히는 날 바로 할 수 있다. Phase 7 의 첫 작업으로 권한다.

### Phase 7 실행 순서

1. 디지털 트윈으로 갭 측정 (4-6) — **먼저 재고 시작한다**
2. 영점·부호 표 확정 (4-1)
3. 측정값으로 도메인 랜덤화 범위 설정 → 시뮬 재학습 (4-3, 4-4)
4. 정책을 젯슨으로 전송, 실물 자율 실행
5. 실물 소량 데이터로 파인튜닝 (4-5)

**✅ 검증**: 시뮬 학습 정책이 실물에서 기본 동작 → 파인튜닝 후 성공률 상승

---

## 5. 지연이 문제가 되면 (참고 — 지금은 아님)

현재 구조는 지령만 인터넷을 건너므로 41ms 지연이 조종을 크게 방해하지 않는다.
그래도 답답하면:

- **RustDesk 화면 지연**이 원인일 가능성이 크다 (영상이라 지령보다 느리다).
  화질/프레임을 낮추면 개선된다.
- 집에 가서 같은 Wi-Fi 로 붙으면 지연이 사라진다. 구조는 그대로다.
- 초판의 대안 A(Brev 클라우드 GPU)는 **지금 구조에서는 이점이 없다.**
  영상을 스트리밍하지 않으므로 데이터센터 회선의 이점이 안 생긴다.

---

## 6. 무엇을 어디서 (한눈에)

| 작업 | 어디서 | 인터넷을 건너나 |
|---|---|---|
| VR 조종 입력 | Quest → 맥북 (같은 Wi-Fi) | ❌ |
| IK · 클러치 · 안전 | 맥북 | ❌ |
| 관절 지령 | 맥북 → 집 데스크탑 | ✅ 숫자만 (~200B/프레임) |
| VR HUD 렌더 | 맥북 로컬 | ❌ |
| 화면 확인 | 집 → 맥북 RustDesk | ✅ 영상 (조종 루프 밖) |
| 데이터 수집·학습·평가 | 집 데스크탑 | ❌ (같은 기계) |
| 실물 추론 (Phase 7) | 작업장 젯슨 | ❌ (로컬) |

---

## 7. 다음에 할 것

1. **Phase 4 착수** — 씬에 큐브, 카메라 2대, lerobot 기록 계층
2. CAN 어댑터 도착 시 **디지털 트윈 갭 측정** (4-6) — Phase 7 을 안 기다리고 먼저
3. 시뮬에 마찰 넣기 (4-3) — 안 넣으면 실물의 정지마찰 구간이 안 보인다
